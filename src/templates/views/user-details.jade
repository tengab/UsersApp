a.btn.btn-primary(ui-sref="users") Indietro
h1 USER DETAILS

user-info
div(ng-model="isMapAvailable")
    user-map(ng-if="isMapAvailable")
    div(ng-if='!isMapAvailable')  
        .panel.panel-default
            #no-location.panel-body
                strong No location data